# DEVELOPMENT ENVIRONMENT CONFIGURATION
# Copy this file to .env for local development
# Or use: cp dev.env .env

# Application Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database Configuration
# For local PostgreSQL (not Docker)
DATABASE_URL=postgresql://crm_user:crm_password@localhost:5432/crm_db

# PostgreSQL Configuration (for local PostgreSQL setup)
POSTGRES_USER=crm_user
POSTGRES_PASSWORD=crm_password
POSTGRES_DB=crm_db
POSTGRES_PORT=5432

# Database Configuration (for Docker Compose)
# Uncomment if using docker-compose
# DATABASE_URL=postgresql://crm_user:crm_password@postgres:5432/crm_db

# JWT Configuration (for future authentication features)
# JWT_SECRET=dev-secret-key-change-in-production
# JWT_EXPIRES_IN=24h
# JWT_REFRESH_SECRET=dev-refresh-secret-key-change-in-production
# JWT_REFRESH_EXPIRES_IN=7d

# Logging Configuration
# LOG_LEVEL=debug
# LOG_FORMAT=json

# API Configuration
# API_VERSION=v1
# API_PREFIX=/api

# CORS Configuration
# Comma-separated list of allowed origins (use * for all in development)
# Example: CORS_ORIGIN=http://localhost:3000,http://localhost:5173,https://yourdomain.com
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Rate Limiting (if needed)
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=60000

